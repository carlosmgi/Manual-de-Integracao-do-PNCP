version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

  jobs:
    post_build:
      - sphinx-build -b singlehtml docs/ $READTHEDOCS_OUTPUT/html/singlehtml/

sphinx:
  configuration: docs/conf.py

  builder: html  
  formats:
    - htmlzip
    - pdf
